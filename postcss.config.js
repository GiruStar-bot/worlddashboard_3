// PostCSS configuration file used by Vite.  It simply declares the
// Tailwind CSS and Autoprefixer plugins.  Tailwind will inject its
// utilities and component classes at build time while Autoprefixer will
// ensure vendor prefixes are applied automatically where necessary.

module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};